const base = require("./script/base");
const globlin = require("./script/globlin");
const player = require("./script/player");

module.exports = {
    base: base,
    globlin: globlin,
    player: player,
    damageArea: damageArea
}